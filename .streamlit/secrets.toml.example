# Optional AI configuration for Streamlit Community Cloud.
# Never rename this example to secrets.toml or commit a populated file.
# Omit the following line to use local-rule parsing and matching only.
OPENAI_API_KEY = ""
OPENAI_MODEL = "gpt-5-mini"
