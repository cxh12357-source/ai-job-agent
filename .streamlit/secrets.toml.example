# Copy these values into Streamlit Community Cloud > Advanced settings > Secrets.
# Never rename this example to secrets.toml and commit the populated file.
APP_ACCESS_PASSWORD = "replace-with-at-least-12-random-characters"

# Optional. Omit this line when you want local-rule parsing and matching only.
OPENAI_API_KEY = ""
OPENAI_MODEL = "gpt-5-mini"
